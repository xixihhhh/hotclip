# HotClip v0.30.0 — 相近台词搜索

## 更容易找回识别不准的台词

- 逐句稿精确搜索没有结果时，可主动开启「相近台词」。
- 在限定长度内查找只差一个字的识别结果，覆盖错字、漏字和多字。
- 近似命中会单独标识，保留原有词级/估算/句级时间来源；试听确认后再选段。
- 中英文界面和 README 同步说明，搜索结果与现有台词和画面证据导航保持一致。

## 验证

- 新增替换、漏字、多字、Unicode 字符和长度边界测试。
- 发布构建将在 macOS、Windows、Linux 原生 runner 上执行完整测试和打包。
